name: prometheus
services:
  prometheus:
    container_name: linko-prometheus
    hostname: linko-prometheus
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
